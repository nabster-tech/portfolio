# Production Environment Configuration
# This file is used when building the application for production

# Base URL for the application in production
# Update this path if your app will be deployed to a subdirectory
VITE_BASE_URL=/portfolio/

# Production specific configurations
NODE_ENV=production
